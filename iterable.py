'''
member를 하나씩 순서대로 반환 가능한 object
for x in object로 접근

sequence type: 문자열 리스트 튜플 
non sequence type: 딕셔너리

라고 iterator인 것은 아님

iter()메소드로 iterator 생성 
next(iterator)로 iterable한 object를 순차적으로 접근 가능
=
for x in object로 접근하는 것은 위의 프로세스가 자동으로 이루어지는 것

'''